# Benchmarks

This directory contains standalone benchmark bundles that are intentionally kept
separate from the Python package code.

- `phisto_extended/`: broad PHISTO bacteria-human benchmark bundle with the
  documented result tables, publication-ready figures, and an interactive
  notebook that reruns the upstream prediction steps through the packaged
  MicrobioLink 2.1 API.
