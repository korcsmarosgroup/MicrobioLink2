[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[project]
authors = [
  {name = "Leila Potari-Gul", email = "l.potari-gul@imperial.ac.uk"},
  {name = "Toby Lawrence", email = "tobylawrence1998@gmail.com"},
]
dependencies = [
    "mygene>=3.2.2",
    "numpy>=1.26.4",
    "omnipath>=1.0.8",
    "pandas>=2.2.2",
    "requests>=2.32.0",
    "scipy>=1.13.1",
]
description = "Prediction of microbe-host protein interactions using domain-motif interactions and intrinsically disordered regions"
license = "BSD-3-Clause"
name = "microbiolink"
readme = "README.md"
requires-python = ">=3.12"
version = "2.1.0"

[project.optional-dependencies]
# Module 10 (microbiolink-enrichment) requires this extra: gget calls the live Enrichr API at
# runtime and matplotlib draws the plot. Both are lazy-imported inside the workflow functions.
enrichment = ["gget>=0.29.0", "matplotlib>=3.8.0"]
idr = ["iupred[aiupred] @ git+https://github.com/saezlab/iupred.git"]
# Module 9 (microbiolink-tiedie) requires this extra: pulls in tiedie (and networkx), Python >=3.9.
tiedie = ["tiedie @ git+https://github.com/saezlab/tiedie.git"]

[project.scripts]
microbiolink-ddi = "microbiolink.cli.ddi:main"
microbiolink-enrichment = "microbiolink.cli.enrichment:main"
microbiolink-dmi = "microbiolink.cli.dmi:main"
microbiolink-download-domains = "microbiolink.cli.domain_download:main"
microbiolink-download-fasta = "microbiolink.cli.fasta_download:main"
microbiolink-idr-filter = "microbiolink.cli.idr_filter:main"
microbiolink-membrane-filter = "microbiolink.cli.membrane_filter:main"
microbiolink-monte-carlo = "microbiolink.cli.monte_carlo:main"
microbiolink-tiedie = "microbiolink.cli.tiedie:main"
microbiolink-zscore-filter = "microbiolink.cli.zscore_filter:main"

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["microbiolink"]

[tool.hatch.build.targets.sdist]
only-include = ["microbiolink", "README.md", "LICENSE"]

[dependency-groups]
dev = [
    "ruff>=0.9",
]
