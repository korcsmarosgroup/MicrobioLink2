% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pair_fastqs.R
\name{pair_fastqs}
\alias{pair_fastqs}
\title{pair_fastqs}
\usage{
pair_fastqs(input_dir, layout)
}
\arguments{
\item{input_dir}{(is.character) Absolute or relative path to the input FASTQ folder.}

\item{layout}{(is.character) Organization style of FASTQ files:
\itemize{
\item 'merged': all FASTQs in a single directory.
\item 'subdir': one subdirectory per sample containing FASTQs.
}}
}
\value{
dict: Nested list of samples and their corresponding FASTQ paths.
}
\description{
Match R1, R2, and optional I1 FASTQ files per sample.
}
\details{
This helper function scans the input directory (or per-sample subdirectories)
and pairs sequencing files for each sample based on filename prefixes. It works
for both 'merged' (all files in one directory) and 'subdir' (one folder per sample) layouts.

Notes:
\itemize{
\item Expects FASTQ filenames to contain '_R1', '_R2', and optionally '_I1'.
\item Does not validate that all paired files exist; just groups them.
\item Returns a dictionary suitable for feeding into STAR/STARsolo commands.
}
}
\keyword{internal}
